{
  "scripts": {
    "dev": "turbo dev",
    "build:production": "turbo build:production",
    "deploy": "npm run build:production && cd apps/backend && fly deploy"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "axios": "^1.4.0",
    "turbo": "^1.10.0"
  }
}
